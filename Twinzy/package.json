{
  "name": "twinzy",
  "version": "0.1.0",
  "private": true,
  "description": "Celebrity lookalike search with rich profile similarity",
  "scripts": {
    "dev": "pnpm --filter @twinzy/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "seed": "pnpm --filter @twinzy/seed run seed"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
